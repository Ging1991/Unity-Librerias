using UnityEngine;

public class YouTubePlayer : MonoBehaviour {
	/*
		private WebViewObject webViewObject;

		void Start() {
			webViewObject = (new GameObject("WebViewObject")).AddComponent<WebViewObject>();
			webViewObject.Init(
				enableWKWebView: true,
				transparent: false,
				zoom: false
			);

			// URL del video de YouTube (modo incrustado)
			string videoId = "RI69hhCVYIc";
			string url = $"https://www.youtube.com/embed/{videoId}?autoplay=1&controls=1";

			// Cargar la URL
			webViewObject.LoadURL(url);
			webViewObject.SetMargins(0, 100, 0, 100); // Ajusta la posición del WebView
			webViewObject.SetVisibility(true);
		}
	*/
}
